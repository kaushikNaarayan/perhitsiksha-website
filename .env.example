# Environment Configuration
VITE_COUNTER_WORKSPACE=your_workspace_name
VITE_ENVIRONMENT=development
VITE_API_BASE_URL=https://api.counterapi.dev/v2

# Optional: Override environment-specific base count
# VITE_COUNTER_BASE_COUNT=350

# Optional: Override cache TTL (time-to-live in milliseconds)
# VITE_COUNTER_CACHE_TTL=300000

# Optional: Google Analytics
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Note: Counter API works without authentication for public counters
# Environment-specific files: .env.development, .env.staging, .env.production
# Base counts: development=10, staging=100, production=350 (configurable)
# Cache TTL: development=2min, staging=5min, production=10min (configurable)