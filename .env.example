# Environment Configuration
VITE_ENVIRONMENT=development

# Supabase Configuration (Recommended for production)
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# Legacy Counter API (Fallback - has daily reset issue)
# VITE_COUNTER_WORKSPACE=your_workspace_name
# VITE_API_BASE_URL=https://api.counterapi.dev/v2
# VITE_COUNTER_BASE_COUNT=350
# VITE_COUNTER_CACHE_TTL=300000

# Optional: Google Analytics
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Note: 
# - Supabase provides cumulative page views that never reset
# - Counter API resets daily (not recommended for cumulative counting)
# - If Supabase is not configured, falls back to Counter API + base count
# Environment-specific files: .env.development, .env.staging, .env.production